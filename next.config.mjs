/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['firebasestorage.googleapis.com','assets-global.website-files.com','ae01.alicdn.com'],
  },
};

export default nextConfig;
